# Future markdown features

This folder contains Markdown samples that are *not yet* supported by the streaming renderer.
Goldens for these files are generated with the goldmark+GFM renderer to document expected
behavior once we implement them.
